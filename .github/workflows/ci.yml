name: My First CI
on:
    push:
        branches:
            -main

    workflow_dispatch:
        
jobs:
    hello:
        runs-on: ubuntu-latest
        steps:    
          - name: say hello world
            run: echo "hello world first"
            
