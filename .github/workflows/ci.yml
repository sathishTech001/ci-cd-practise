name: My First CI
on:
    push:
        branches:
            -main
jobs:
    hello:
        runs-on: ubuntu-latest
        steps:    
          - name: say hello world
            run: echo "hello world ."
            
